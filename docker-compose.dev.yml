services:
  # Настройки PHP для разработки (монтирование кода)
  laravel-php-httpd-tcp:
    volumes:
      - .:/var/www/laravel

  # Настройки Httpd для разработки
  laravel-httpd-tcp:
    ports:
      - "${HTTPD_PORT}:80"
    volumes:
      - .:/var/www/laravel
    depends_on:
      - laravel-node-httpd-tcp

  # Node.js для сборки фронтенда и Hot Module Replacement (HMR)
  laravel-node-httpd-tcp:
    image: node:24-alpine
    container_name: laravel-node-httpd-tcp
    working_dir: /var/www/laravel
    volumes:
      - .:/var/www/laravel
      - node_modules:/var/www/laravel/node_modules
    command: >
      sh -lc '
        # Установка зависимостей, если они отсутствуют
        if [ -f package-lock.json ]; then
          npm ci;
        else
          npm install;
        fi;
      
        # Запуск Vite в режиме разработки
        npm run dev -- --host 0.0.0.0 --port 5173
      '
    ports:
      - "${VITE_PORT:-5173}:5173"
    networks:
      - laravel-httpd-tcp-network

  # Только для разработки: проброс портов PostgreSQL и Redis наружу
  laravel-postgres-httpd-tcp:
    ports:
      - "${DB_FORWARD_PORT}:5432"

  laravel-redis-httpd-tcp:
    ports:
      - "${REDIS_FORWARD_PORT}:6379"

  # Только для разработки: интерфейс управления БД (pgAdmin)
  laravel-pgadmin-httpd-tcp:
    image: dpage/pgadmin4:latest
    container_name: laravel-pgadmin-httpd-tcp
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
    ports:
      - "${PGADMIN_PORT}:80"
    networks:
      - laravel-httpd-tcp-network
    healthcheck:
      test: [ "CMD", "wget", "-q", "-O", "-", "http://localhost/" ]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 10s
    depends_on:
      laravel-postgres-httpd-tcp:
        condition: service_healthy

volumes:
  node_modules:
